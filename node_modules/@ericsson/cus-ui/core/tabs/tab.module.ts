import { NgModule, ModuleWithProviders } from '@angular/core';
import { ApplicationTabService } from './services';

@NgModule({
    providers: [],
    entryComponents: []
})

export class TabModule {
    static forRoot(): ModuleWithProviders {
        return {
            ngModule: TabModule,
            providers: [ApplicationTabService]
        };
    }
}
