import { Observable } from 'rxjs/Rx';

export abstract class SessionStrategy {
    abstract init(): void;
    abstract logout(showConfirmation: boolean): void;
    abstract getExpiry(): Observable<any>;
}
