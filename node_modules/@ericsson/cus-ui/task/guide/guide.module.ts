import { CommonModule } from '@angular/common';
import { NgModule } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { OdenModule } from '@ericsson/oden/modules';

import { LoggerModule } from '../../logger';
import { TaskModule } from '../task.module';
import { CoreModule } from './../../core';
import { GuideComponent, SummaryComponent, TaskErrorComponent, GuideChildComponent } from './components';
import { GuideRoutingModule } from './guide.routing';
import { CanDeactivateGuard } from './services/can.deactivate.service';
import { SummaryService } from './services/summary.service';

@NgModule({
    providers: [CanDeactivateGuard, SummaryService],
    imports: [
        GuideRoutingModule,
        CoreModule,
        OdenModule,
        LoggerModule,
        CommonModule,
        FormsModule,
        TaskModule],
    declarations: [
        GuideComponent,
        GuideChildComponent,
        SummaryComponent,
        TaskErrorComponent
    ],
    entryComponents: []
})

export class GuideModule {
}
