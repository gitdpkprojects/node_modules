import { ITabsItem } from '@ericsson/oden/core';

export class TabsItem implements ITabsItem {
    constructor( public id: number, public title: string, public path?: string, public active?: boolean) {}
}
