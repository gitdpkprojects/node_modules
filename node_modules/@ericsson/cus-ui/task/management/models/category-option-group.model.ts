import { OptionItem } from '@ericsson/oden/core';

export class CategoryOptionGroup {
    label: string;
    field: string;
    items: Array<OptionItem>;

    constructor(obj: Partial<CategoryOptionGroup> = {}) {
        Object.assign(this, obj);
    }
}
