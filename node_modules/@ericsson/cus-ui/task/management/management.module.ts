import { NgModule } from '@angular/core';
import { BrowserModule } from '@angular/platform-browser';
import { BrowserAnimationsModule } from '@angular/platform-browser/animations';
import { OdenModule } from '@ericsson/oden';

import { CommonModule } from '../../common';
import { CoreModule } from '../../core';
import { LoggerModule } from '../../logger';
import { TaskDetailsComponent, TaskListComponent, TaskContainerComponent } from './components';
import { TaskDataFilterService, TaskManagementService } from './services';

@NgModule({
  imports: [
    OdenModule,
    LoggerModule,
    BrowserAnimationsModule,
    CoreModule,
    BrowserModule,
    CommonModule
  ],
  declarations: [
    TaskDetailsComponent, TaskListComponent, TaskContainerComponent
  ],
  providers: [TaskDataFilterService, TaskManagementService],
  entryComponents: [TaskListComponent, TaskDetailsComponent, TaskContainerComponent]
})
export class ManagementModule { }
