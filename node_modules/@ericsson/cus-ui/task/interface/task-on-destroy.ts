export interface TaskOnDestroy {


    /**
     * Task Lifecycle hook that is called after angular ngOnDestroy
     * `taskOnDestroy` callback is typically used for any custom cleanup that needs to occur when the
     * instance is destroyed.
     */
    taskOnDestroy(): void;
}
