import { TaskContextService } from '../guide/services';

export interface TaskOnPause {

    /**
     * Task Lifecycle hook that is called when screen information is paused and needs to be saved
     */
    taskOnPause(context: TaskContextService): void;
}
