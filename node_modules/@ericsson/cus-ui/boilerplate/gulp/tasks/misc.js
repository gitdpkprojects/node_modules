const gulp = require('gulp');
const del = require('del');
const conf = require('../config');

gulp.task('clean', clean);

/* clean target folder for a new build */
function clean() {
  return del([conf.paths.dist, conf.paths.tmp]);
}
