/*tslint: disable*/
import { Injectable } from '@angular/core';
import { ContextData } from './models/context-data';

@Injectable()
export class ContextService {
    private _dataStore: Array<ContextData>;
    constructor() {
        this._dataStore = [];
    }
    //  /**
    //   * get context data
    //   */
    // private getData(): ContextData {
    //     let contextData = null;
    //     let currentRoutePath = this.getCurrentRoute();

    //     if (this._dataStore[currentRoutePath]) {
    //         contextData = this._dataStore[currentRoutePath];
    //     }

    //     return contextData;
    // }

    // /**
    // * add context data
    // */
    // private addData(forRoute: string, data: any): void {

    //     if (this._dataStore[forRoute]) {
    //         this._dataStore[forRoute].data = data;
    //     } else {
    //         let contextData = new ContextData();
    //         contextData.data = data;
    //         contextData.path = forRoute;
    //         this._dataStore.push(contextData);
    //     }
    // }

    // /**
    //  *  get current url path
    //  */
    // private getCurrentRoute() {
    //     let url = window.location.pathname;
    //     return url;
    // }
}
