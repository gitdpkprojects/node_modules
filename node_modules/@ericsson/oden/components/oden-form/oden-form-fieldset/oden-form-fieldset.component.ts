import { Component, ViewEncapsulation, HostBinding, Input } from '@angular/core';

@Component({
    selector: 'oden-form-fieldset',
    templateUrl: 'oden-form-fieldset.component.html',
    encapsulation: ViewEncapsulation.None,
})
export class OdenFormFieldsetComponent {
    @HostBinding('class.oden-form-fieldset') _hasOdenFormFieldsetClass: boolean = true;
    @HostBinding('class.tight-group') @Input() tightGroup: boolean = false;
    @HostBinding('class.spacer-group') @Input() spacerGroup: boolean = false;
}
