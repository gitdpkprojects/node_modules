import { CommonModule } from '@angular/common';
import { TranslateModule } from '@ngx-translate/core';
import { NgModule, ModuleWithProviders } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { EntityVersionComponent } from './entity-version.component';
import { StateBadgeModule } from '../state-badge/state-badge.module';
import { FormModule } from '../form/form.module';
import { IconModule } from '../icon';
import { TooltipModule } from '../tooltip/tooltip.module';

@NgModule({
    imports: [CommonModule, FormsModule, IconModule, TooltipModule,
        StateBadgeModule, FormModule, TranslateModule.forChild()],
    exports: [EntityVersionComponent],
    declarations: [EntityVersionComponent],
})

export class EntityVersionModule {
    static forRoot(): ModuleWithProviders {
        return {
            ngModule: EntityVersionModule
        };
    }
}
