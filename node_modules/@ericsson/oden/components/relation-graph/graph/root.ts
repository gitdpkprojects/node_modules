import { Ref } from '../relation-graph.model';
import { RelationGraphComponentType } from './type';
import { RelationGraphComponentNode } from './node';
import { RelationGraphComponentGrid } from './grid';
import { RelationGraphComponentPosition } from './position';

export interface RelationGraphComponentRoot {

    readonly translate: RelationGraphComponentPosition;

    readonly grid: RelationGraphComponentGrid;

    readonly rootNode: RelationGraphComponentNode;

    readonly maxDepth: number;

    findType(ref: Ref): RelationGraphComponentType;

    findNode(ref: Ref): RelationGraphComponentNode;

    onFocus(node: RelationGraphComponentNode): void;

    onBlur(): void;

    onClick(node: RelationGraphComponentNode): void;
}
