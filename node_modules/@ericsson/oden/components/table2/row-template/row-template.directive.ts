import { Directive, TemplateRef } from '@angular/core';

@Directive({
    // tslint:disable-next-line:directive-selector
    selector: '[odenRowTemplate]'
})
export class RowTemplateDirective {
    constructor(public templateRef: TemplateRef<any>) {}
}
