import { Component, HostBinding, Input, ViewEncapsulation } from '@angular/core';
import { BlockTreeBranch } from '../../../core/models/block-tree-branch.model';
import { BlockTreeBranchLeaf } from '../../../core/models/block-tree-branch-leaf.model';

@Component({
    selector: 'cui-rule-tree',
    templateUrl: 'rule-tree.component.html',
    styleUrls: [ 'rule-tree.component.scss' ],
    encapsulation: ViewEncapsulation.None
})
export class RuleTreeComponent {
    @HostBinding('class.cui-rule-tree') addClass: boolean = true;
    @Input() model: BlockTreeBranch | BlockTreeBranchLeaf;

    blockTreeBranch = BlockTreeBranch;
    blockTreeBranchLeaf = BlockTreeBranchLeaf;
}
