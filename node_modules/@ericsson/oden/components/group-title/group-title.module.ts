import { CommonModule } from '@angular/common';
import { ModuleWithProviders, NgModule } from '@angular/core';

import { IconModule } from '../icon/icon.module';
import { StateBadgeModule } from '../state-badge/state-badge.module';
import { PipesModule } from './../../core/pipes/pipes.module';
import { GroupTitleComponent } from './group-title.component';
import { TooltipModule } from '../tooltip/tooltip.module';

@NgModule({
    imports: [CommonModule, IconModule, StateBadgeModule, PipesModule, TooltipModule],
    exports: [GroupTitleComponent],
    declarations: [GroupTitleComponent]
})

export class GroupTitleModule {
    static forRoot(): ModuleWithProviders {
        return {
            ngModule: GroupTitleModule
        };
    }
}
