import { Directive, HostBinding } from '@angular/core';

@Directive({
    selector: '[cui-table-sorting]',
})
export class TableSortingDirective {
    @HostBinding('class.sorting') addClass: boolean = true;
}
