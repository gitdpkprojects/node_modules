// tslint:disable:component-selector
import { Component, Input, ViewEncapsulation } from '@angular/core';

@Component({
    selector: '[cui-table-cell-version]',
    templateUrl: 'table-cell-version.component.html',
    encapsulation: ViewEncapsulation.None
})

export class TableCellVersionComponent {
    @Input() extra: number;
    @Input() label: string;
    @Input() tooltip: string;
    constructor() {
    }
}
