import { Component, ViewEncapsulation, HostBinding, Input } from '@angular/core';

@Component({
    selector: 'cui-table-cell-meta',
    templateUrl: './table-cell-meta.component.html',
    styleUrls: ['./table-cell-meta.component.scss'],
    encapsulation: ViewEncapsulation.None
})

export class TableCellMetaComponent {
    @HostBinding('class.cui-table-cell-meta') addClass: boolean = true;
    @HostBinding('class.cellwrap') @Input() cellWrap: boolean = false;
}
