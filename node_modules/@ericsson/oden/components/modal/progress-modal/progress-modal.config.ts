import { ModalActionItem } from '../../../core/models/button-action-item.model';
import { ProgressBarConfig } from '../../form/progress-bar/progress-bar.config';

export class ProgressModalConfig extends ProgressBarConfig {
    actions: ModalActionItem[] = [];
}

/** @deprecated Still here because we don't want a breaking change */
export class ProgressConfig extends ProgressModalConfig { }
