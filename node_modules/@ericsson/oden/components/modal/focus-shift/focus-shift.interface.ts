import { IModal } from '../modal.interface';
import { ComponentRef } from '@angular/core';
import { DrawerComponent } from '../../drawer/drawer.component';
import { MultiDrawerComponent } from '../../drawer/multi-drawer/multi-drawer.component';

export interface IFocusShift extends IModal {
    zIndex: number;
    drawerType?: any;
    drawer?: ComponentRef<DrawerComponent<any>>;
    drawerTypes?: Array<any>;
    drawers?: Array<ComponentRef<MultiDrawerComponent>>;
    setClass(string): void;
}
