import { Component, OnInit } from '@angular/core';
import { CharacteristicTypeBase } from '../../characteristic-type-base';
import { CharacteristicService } from '../../characteristic.service';
import { TranslateService } from '@ngx-translate/core';

@Component({
  selector: 'cui-characteristics-time',
  templateUrl: 'time.component.html'
})
export class TimeTypeComponent extends CharacteristicTypeBase implements OnInit {

  constructor(characteristicService: CharacteristicService,
    translate: TranslateService) {
    super(characteristicService, translate);
  }

  ngOnInit() {
    this.triggerView();
  }
}
