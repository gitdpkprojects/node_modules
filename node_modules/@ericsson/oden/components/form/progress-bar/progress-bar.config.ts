/** @deprecated */
export class ProgressBarConfig {
    progress: number = 0; // Should be between 0-100;
    title: string;
    subtitle: string;
    max?: number;
    unit?: string;
    showTitle?: boolean;
    constructor(obj: Partial<ProgressBarConfig> = {}) {
        Object.assign(this, obj);
    }
}
