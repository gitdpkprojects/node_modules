import {
    Component,
    HostBinding,
    Input,
    ViewEncapsulation
} from '@angular/core';

@Component({
    selector: 'cui-tile-section-more',
    templateUrl: 'tile-section-more.component.html',
    styleUrls: ['tile-section-more.component.scss'],
    encapsulation: ViewEncapsulation.None
})

export class TileSectionMoreComponent {
    @HostBinding('class.cui-tile-section-more') addClass: boolean = true;
    @Input() tooltip: string = null;
    @Input() items: number = 0;
}
