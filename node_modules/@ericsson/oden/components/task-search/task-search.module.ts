import { CommonModule } from '@angular/common';
import { NgModule, ModuleWithProviders } from '@angular/core';
import { TranslateModule } from '@ngx-translate/core';
import { TaskSearchComponent } from './task-search.component';
import { FormModule } from '../form/form.module';
import { ButtonModule } from '../button/button.module';
import { FormsModule } from '@angular/forms';
import { TableModule } from '../table/table.module';

@NgModule({
    imports: [CommonModule, FormModule, FormsModule, ButtonModule, TableModule, TranslateModule.forChild()],
    exports: [TaskSearchComponent],
    declarations: [TaskSearchComponent]
})

export class TaskSearchModule {
    static forRoot(): ModuleWithProviders {
        return {
            ngModule: TaskSearchModule
        };
    }
}
