import { ComponentRef, Type } from '@angular/core';
import { UUID } from '../../core/helpers/uuid.class';
export class UniversalTab {
    id: string = UUID.create();
    name: string;
    identifier: any;
    icon: string;
    order: number;
    hotkey?: string;
    component: Type<any>;
    componentRef?: ComponentRef<any>;
    inputs?: { [s: string]: any; };

    constructor(obj: {
        name?: string,
        identifier?: any;
        icon?: string,
        order?: number,
        hotkey?: string,
        component?: Type<any>,
        componentRef?: ComponentRef<any>,
        inputs?: { [s: string]: any; }
    } = {}) {
        Object.assign(this, obj);
    }
}

