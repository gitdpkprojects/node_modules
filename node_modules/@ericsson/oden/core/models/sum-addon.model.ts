import { Addon } from './addon.model';
import { CHART_ADDON } from '../constants/chart-addons.constants';

export class SumAddon extends Addon {
    type: CHART_ADDON = CHART_ADDON.SUMMARY;
    label: string;
    unit: string;
    value: number;
    format?: string;
    sum?: number;
}
