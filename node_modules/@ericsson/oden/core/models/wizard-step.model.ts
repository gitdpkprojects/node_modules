import { QuickAssign } from '../helpers/quickassign.model';

export class WizardStep extends QuickAssign {
    optional: boolean = false;
    completed: boolean = false;
    locked: boolean = false;
}
