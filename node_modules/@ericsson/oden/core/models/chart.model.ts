import { UUID } from '../helpers/uuid.class';
import { QuickAssign } from '../helpers/quickassign.model';

export class ChartData extends QuickAssign {
    value: number;
    label: string;
    id: string = `cd-${UUID.create()}`;
    unit: string;
    date: Date;
    data: any;
    parent: ChartData;
    color?: string;
    link?: string;
    misc?: Array<ChartData>;
    selected?: boolean;
    fractionDigits?: number;
}
