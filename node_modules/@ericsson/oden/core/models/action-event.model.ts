import { ActionItem } from './action-item.model';
import { ElementRef } from '@angular/core';

export class ActionEvent extends ActionItem {
    target: {elementRef: ElementRef};
    constructor(obj: any) {
        super(obj);
        this.target = obj.target;
    }
}
