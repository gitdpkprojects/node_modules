import { STATE, COLOR, TIME } from '../constants';
import { QuickAssign } from '../helpers/quickassign.model';
export class EntityVersion extends QuickAssign {
    state: STATE;
    label: string;
    selected: boolean;
    extra: number;
    tooltip: string;
    color: COLOR;
    outlined: boolean;
    semiTransparent: boolean;
    validFrom?: Date | TIME;
    validTo?: Date | TIME;
}

/**
 * @deprecated use EntityVersion instead.
 */
export class StateBatch extends EntityVersion {}
