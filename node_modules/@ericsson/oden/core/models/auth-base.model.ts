export class AuthBase {
    tpgName: string;
    resource: string;
    operation: string;
    version?: string;
    constructor(obj: {
        tpgName?: string,
        resource?: string,
        operation?: string,
        version?: string
    } = {}) {
        Object.assign(this, obj);
    }
}
