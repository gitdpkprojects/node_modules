import { Addon } from './addon.model';
import { CHART_ADDON } from '../constants/chart-addons.constants';
import { OptionItem } from './option-item.model';

export class SelectorAddon extends Addon {
    type: CHART_ADDON = CHART_ADDON.SELECTOR;
    options: Array<OptionItem>;
    onChange: (data: Event) => void;
    label: string;
    id: string;
}
