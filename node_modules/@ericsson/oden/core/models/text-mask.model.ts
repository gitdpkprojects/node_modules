import { QuickAssign } from '../helpers/quickassign.model';

export class TextMaskConfig extends QuickAssign {
    mask: Array<any>;
    keepCharPositions: boolean;
    guide: boolean;
    placeholderChar: string;
    pipe: any;
}
