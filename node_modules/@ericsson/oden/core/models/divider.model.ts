/**
 * This can be displayed in a context menu
 */
export class Divider {
    divider: boolean = true;
    disabled: boolean = true;
    label?: string;

    constructor(obj: {
        divider?: boolean;
        label?: string
    } = {}) {
        Object.assign(this, obj);
    }
}
