export class TimeHelper {
    /**
     * Waits for X ms
     * @param delay
     */
    static wait(delay: number = 0) {
        return new Promise(resolve => {
            setTimeout(() => resolve(), delay);
        });
    }
}
