import { Injectable } from '@angular/core';
import { BehaviorSubject } from 'rxjs';
import { IWindowSize } from '../../interfaces/window-size';
import { SIZING } from '../../constants/sizing.constants';

@Injectable()
export class ResizeServiceStub {
    readonly resize$: BehaviorSubject<IWindowSize>;
    constructor() {
        this.resize$ = new BehaviorSubject({
            width: 1024,
            height: 900,
            size: SIZING.MEDIUM_SCREEN
        });
    }
}
