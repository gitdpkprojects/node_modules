import { DRAWER_POSITION } from '../constants/drawer.constants';

export interface IMultiDrawerContent {
    drawerIcon: string;
    drawerTooltip: string;
    drawerWidth?: number;
    drawerPosition?: DRAWER_POSITION;
    /** Determines if drawer should display maximize button, default is true if drawer contains library, otherwise false */
    drawerMaximization?: boolean;
    onShow?(): void;
    onHide?(): void;
}
