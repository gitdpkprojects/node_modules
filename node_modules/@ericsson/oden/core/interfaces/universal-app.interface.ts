export interface IUniversalApp {
    id: string;
    name: string;
    category: string;
    favorite: boolean;
    order: number;
    path: string;
    newWindow?: boolean;
    routes: Array<{
        label: string;
        path: string;
        order: number;
        newWindow?: boolean;
        externalApp?: string;
        favorite: boolean;
    }>;
}
