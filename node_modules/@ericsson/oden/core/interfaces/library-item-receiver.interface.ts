import { TactileBlockItem } from '../models';

/**
 * Implemented by all library item receiver.
 */
export interface ILibraryItemReceiver {
    name: string;

    canReceiveLibraryItem(tactileItem: TactileBlockItem): boolean;

    onReceiveLibraryItem(tactileItem: TactileBlockItem): void;
}
