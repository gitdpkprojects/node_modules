import { ITabs, ITabsItem } from './tabs.interface';
import { ComponentFactoryResolver, EventEmitter } from '@angular/core';

export interface IPaneContent {
    open?: boolean;
    componentFactoryResolver?: ComponentFactoryResolver;
    zIndex?: number;
    tabs?: ITabs;
    activeTab?: EventEmitter<ITabsItem>;
}
