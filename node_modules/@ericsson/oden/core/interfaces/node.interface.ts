import { NodeViewComponent } from '../../components/node-tree/node-view/node-view.component';
import { Type, TemplateRef } from '@angular/core';
export type Nodes = Array<INode>;

export interface INode {
    label: string;
    uuid?: string;
    children?: Nodes;
    dynamicChildren?: boolean;
    active?: boolean;
    open?: boolean;
    component?: NodeViewComponent;
    hidden?: boolean;
    icon?: string;
    activeParent?: boolean;
    template?: Type<any> | TemplateRef<any>;
    statusIcon?: string;
}
